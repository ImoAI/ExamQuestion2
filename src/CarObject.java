public class CarObject {
    public void carObject() {
        Car volvo = new Car();
        volvo.speedUp(10);
        volvo.changeCadence(50);
        volvo.changeGear(2);
        volvo.printStates();
    }
}
